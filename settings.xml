<settings>
    <mirrors>
        <mirror>
            <id>central</id>
            <name>Central Mirror Repository</name>
            <url>${repo.central.url}</url>
            <mirrorOf>central</mirrorOf>
        </mirror>
    </mirrors>

    <repositories>
        <repository>
            <id>maven-group</id>
            <url>http://192.168.205.104:8081/repository/maven-public/</url>
        </repository>
    </repositories>

    <servers>
        <server>
            <id>nexus-snapshots</id>
            <username>${repo.username}</username>
            <password>${repo.password}</password>
        </server>
        <server>
            <id>nexus-releases</id>
            <username>${repo.username}</username>
            <password>${repo.password}</password>
        </server>
        <server>
            <id>central</id>
            <username>${repo.username}</username>
            <password>${repo.password}</password>
        </server>
        <server>
            <id>maven-group</id>
            <username>${repo.username}</username>
            <password>${repo.password}</password>
        </server>
    </servers>
    
    <distributionManagement>
        <repository>
            <id>releases</id>
            <url>${repo.release.url}</url>
        </repository>
        <snapshotRepository>
            <id>snapshots</id>
            <url>${repo.snapshot.url}</url>
        </snapshotRepository>
    </distributionManagement>
</settings>
